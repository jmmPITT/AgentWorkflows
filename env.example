# Google Cloud Platform Configuration
# Get your project ID from the Google Cloud Console
GCP_PROJECT_ID=your-gcp-project-id

# Google Cloud region (us-central1, us-east1, etc.)
GCP_REGION=us-central1

# Serper API Key for web search verification
# Get your free API key from: https://serper.dev/
SERPER_API_KEY=your-serper-api-key

# Optional: Google Cloud Service Account JSON (if not using Application Default Credentials)
# GCP_SERVICE_ACCOUNT_JSON_B64=base64-encoded-json-key

# Optional: Custom model configuration
# LLM_TEMPERATURE=0.3
# LLM_MODEL=vertex_ai/gemini-2.5-flash
